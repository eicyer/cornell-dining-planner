services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cornell_dining
    ports:
      - "5433:5432"
    volumes:
      - cornell_dining_pgdata:/var/lib/postgresql/data

volumes:
  cornell_dining_pgdata:
