# Human-edited source of truth for direct dependencies. Run
#   pip-compile --generate-hashes requirements.in
# to regenerate the hash-pinned requirements.txt after changing this file.
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.12
pydantic-settings==2.6.1
authlib==1.6.12
httpx==0.27.2
itsdangerous==2.2.0
python-multipart==0.0.31
anthropic==0.40.0
jinja2==3.1.6
slowapi==0.1.9

# starlette (transitive, via fastapi) is pinned by fastapi==0.115.0's own
# constraint (starlette>=0.37.2,<0.39.0) to 0.38.6, which has known CVEs
# with no fix release inside that range — every fix version is >=0.40.0.
# Patching it means upgrading fastapi itself, a bigger change than this
# pass takes on unreviewed. Documented as accepted risk in docs/adr/0018
# until a FastAPI upgrade is planned.
